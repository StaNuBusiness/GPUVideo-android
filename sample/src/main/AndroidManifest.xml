<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.daasuu.gpuvideoandroid">

    <!-- Camera permissions -->
    <uses-feature android:name="android.hardware.camera" />
    <uses-feature android:name="android.hardware.camera.autofocus" />

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!-- MainActivity requires android:exported because of the intent-filter -->
        <activity
            android:name=".MainActivity"
            android:exported="true"> <!-- Set to true because it has an intent-filter -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- No intent-filters, but it's good practice to specify exported -->
        <activity
            android:name=".PlayerActivity"
            android:screenOrientation="portrait"
            android:exported="false" /> <!-- Not accessible by other apps -->

        <activity
            android:name=".CameraSelectActivity"
            android:screenOrientation="portrait"
            android:exported="false" /> <!-- Not accessible by other apps -->

        <activity
            android:name=".PortraitCameraActivity"
            android:screenOrientation="portrait"
            android:exported="false" /> <!-- Not accessible by other apps -->

        <activity
            android:name=".LandscapeCameraActivity"
            android:screenOrientation="landscape"
            android:exported="false" /> <!-- Not accessible by other apps -->

        <activity
            android:name=".SquareCameraActivity"
            android:exported="false" /> <!-- Not accessible by other apps -->

        <activity
            android:name=".Mp4ComposeActivity"
            android:exported="false" /> <!-- Not accessible by other apps -->

    </application>

</manifest>
